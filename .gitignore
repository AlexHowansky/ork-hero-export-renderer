dist/
node_modules/
# Anchored: this is the generated rules data at the repository root.
# Unanchored it would also swallow src/rules/, which is source.
/rules/
*.tgz
*.tsbuildinfo

# HERO Designer's own program jar and manual: Hero Games' copyrighted material,
# not ours to redistribute. See README "Running the tests".
fixtures/HD6.jar
fixtures/HDDocs.pdf
